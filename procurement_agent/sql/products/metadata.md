# products

- 表用途：产品主数据；用于把采购需求中的产品、SKU 与内部 `product_id` 对齐。
- `id`：产品内部主键；跨表关联时使用。
- `sku`：唯一库存编码；用户提供 SKU 或需要稳定识别产品时使用。
- `name`：产品名称；展示和名称匹配时使用。
- `category`：产品分类；非固定值，表示企业商品分类。
- `unit`：计量单位；非固定值，数量字段必须按该单位解释。
- `quality_standard`：质量要求文本；可为空，筛选或输出质量约束时使用。
