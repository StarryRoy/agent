# inventory

- 表用途：产品当前库存快照；用于计算可用库存、预计可用量和采购缺口。
- `product_id`：产品主键，同时关联 `products.id`；每个产品仅一条当前快照。
- `current_qty`：账面现存数量，非负整数。
- `locked_qty`：已被其他需求占用、不可用于本次需求的数量，非负整数。
- `in_transit_qty`：已下单但尚未入库的数量，非负整数。
- `safety_stock`：计算后仍应保留的安全库存，非负整数。
- `updated_at`：快照更新时间，ISO-8601 日期或时间文本；判断数据时效时使用。
