[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "enterprise-procurement-agent"
version = "0.1.0"
description = "A Harness-based multi-agent procurement decision and execution app"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "agent-harness @ git+https://github.com/StarryRoy/harness.git@codex",
  "langchain-core>=0.3.30,<2",
  "langgraph>=0.2.60,<2",
  "langgraph-checkpoint-sqlite>=3,<4",
  "langchain-mcp-adapters>=0.1,<1",
  "pydantic>=2.7,<3",
  "fastapi>=0.115,<1",
  "uvicorn>=0.30,<1",
]

[project.optional-dependencies]
dev = ["pytest>=8,<9", "ruff>=0.9,<1", "httpx>=0.27,<1"]

[project.scripts]
procurement-agent = "procurement_agent.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["procurement_agent", "backend"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 100
